<?php

namespace Tests;

trait TestParameter
{
    /**
     * Provides an array with valid data.
     *
     * @param array $overrides
     * @return array
     */
    protected function validCategoryParams(array $overrides = []): array
    {
        return array_merge([
            // TODO: Implement data
        ], $overrides);
    }

    /**
     * Provides an array with valid data.
     *
     * @param array $overrides
     * @return array
     */
    protected function validCollectionParams(array $overrides = []): array
    {
        return array_merge([
            // TODO: Implement data
        ], $overrides);
    }

    /**
     * Provides an array with valid data.
     *
     * @param array $overrides
     * @return array
     */
    protected function validEpisodeParams(array $overrides = []): array
    {
        return array_merge([
            // TODO: Implement data
        ], $overrides);
    }

    /**
     * Provides an array with valid data.
     *
     * @param array $overrides
     * @return array
     */
    protected function validMovieParams(array $overrides = []): array
    {
        return array_merge([
            // TODO: Implement data
        ], $overrides);
    }

    /**
     * Provides an array with valid data.
     *
     * @param array $overrides
     * @return array
     */
    protected function validSerieParams(array $overrides = []): array
    {
        return array_merge([
            // TODO: Implement data
        ], $overrides);
    }
}
